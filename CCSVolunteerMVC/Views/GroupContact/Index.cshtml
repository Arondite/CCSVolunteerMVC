@model IEnumerable<CCSVolunteerMVC.Models.GroupContact>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.grpContName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.contactTypeID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.volunteerGroupID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.grpContInfo)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.grpContName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.contactTypeID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.volunteerGroupID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.grpContInfo)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.groupContactID }) |
            @Html.ActionLink("Details", "Details", new { id=item.groupContactID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.groupContactID })
        </td>
    </tr>
}

</table>
