@model IEnumerable<CCSVolunteerMVC.Models.ContactType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.contTypeName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.contTypeName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.contactTypeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.contactTypeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.contactTypeID })
        </td>
    </tr>
}

</table>
